{
  "name": "marussialamy",
  "version": "1.0.0",
  "description": "Scripts for marussialamy theme",
  "main": "index.js",
  "scripts": {
    "sass": "sass --watch ./assets/sass/main.scss:./style-raw.css",
    "sass-min": "sass --watch --style compressed ./assets/sass/main.scss:./style.css",
    "prefix": "postcss ./style.css --use autoprefixer -d ./"
  },
  "author": "Guillaume Marteau",
  "license": "ISC",
  "browserslist": "last 4 versions",
  "devDependencies": {
    "sass": "^1.44.0"
  },
  "dependencies": {
    "@swup/scripts-plugin": "^1.1.0",
    "swup": "^2.0.14"
  }
}
